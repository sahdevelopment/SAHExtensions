{
  "type": "module",
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "esbuild": "^0.28.2",
    "eslint": "^10.8.1",
    "eslint-plugin-userscripts": "^0.5.6",
    "globals": "^17.11.0"
  },
  "allowScripts": {
    "esbuild@0.28.2": true
  },
  "scripts": {
        "lint": "eslintnpm src",
        "build": "npm install && node build.js",
        "dev": "npm install && node dev.js",
        "check": "npm install && npm run lint && npm run build"
    }
}
